#import and use of usual python modules
import os

from shellpython import config as config
config.PRINT_ALL_COMMANDS = True
config.COLORAMA_ENABLED = True

result = i`ping -c 3 8.8.8.8

for line in result.stdout:
    print line

result = ip`example/hello.py
result.sreadline()
result.swriteline('Alexander')
result.sreadline()

print result.returncode

result = ie`ls -l /non_existent_directory
result.stderr.sreadline()