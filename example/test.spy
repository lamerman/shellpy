import os
from example.commoncode.my import common_func

def main():
    for line in `ls -l`:
        s = `echo piggibank: {line} | grep shellpy
        if len(str(s)) > 0:
            print s
            print s.returncode

    print `echo 1` == `echo 1`

    print `ls -l
    print os.ST_APPEND
    print common_func()

if __name__ == '__main__':
    main()